interface:
  display_name: "Admission Control"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $admission-control for this task."

policy:
  allow_implicit_invocation: false
