interface:
  display_name: "Agentic Workflows"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $agentic-workflows for this task."

policy:
  allow_implicit_invocation: true
