interface:
  display_name: "Apollo Router"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $apollo-router for this task."

policy:
  allow_implicit_invocation: false
