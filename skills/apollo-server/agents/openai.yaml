interface:
  display_name: "Apollo Server"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $apollo-server for this task."

policy:
  allow_implicit_invocation: false
