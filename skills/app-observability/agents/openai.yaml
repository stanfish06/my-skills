interface:
  display_name: "App Observability"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $app-observability for this task."

policy:
  allow_implicit_invocation: false
