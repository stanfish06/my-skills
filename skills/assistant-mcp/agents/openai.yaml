interface:
  display_name: "Assistant Mcp"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $assistant-mcp for this task."

policy:
  allow_implicit_invocation: false
