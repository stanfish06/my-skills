interface:
  display_name: "Chdb Datastore"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $chdb-datastore for this task."

policy:
  allow_implicit_invocation: false
