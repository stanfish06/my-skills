interface:
  display_name: "Clickstack Otel Collector"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $clickstack-otel-collector for this task."

policy:
  allow_implicit_invocation: false
