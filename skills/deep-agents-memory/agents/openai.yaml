interface:
  display_name: "Deep Agents Memory"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $deep-agents-memory for this task."

policy:
  allow_implicit_invocation: false
