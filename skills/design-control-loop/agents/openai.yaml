interface:
  display_name: "Design Control Loop"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $design-control-loop for this task."

policy:
  allow_implicit_invocation: false
