interface:
  display_name: "Firecrawl Crawl"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-crawl for this task."

policy:
  allow_implicit_invocation: false
