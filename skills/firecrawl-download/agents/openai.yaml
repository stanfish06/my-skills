interface:
  display_name: "Firecrawl Download"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-download for this task."

policy:
  allow_implicit_invocation: false
