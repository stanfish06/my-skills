interface:
  display_name: "Firecrawl Map"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-map for this task."

policy:
  allow_implicit_invocation: false
