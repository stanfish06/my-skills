interface:
  display_name: "Firecrawl Monitor"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-monitor for this task."

policy:
  allow_implicit_invocation: false
