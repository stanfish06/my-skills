interface:
  display_name: "Firecrawl Scrape"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-scrape for this task."

policy:
  allow_implicit_invocation: false
