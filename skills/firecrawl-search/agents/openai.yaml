interface:
  display_name: "Firecrawl Search"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $firecrawl-search for this task."

policy:
  allow_implicit_invocation: false
