interface:
  display_name: "Grafana Scenes"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $grafana-scenes for this task."

policy:
  allow_implicit_invocation: false
