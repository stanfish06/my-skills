interface:
  display_name: "Incident Response"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $incident-response for this task."

policy:
  allow_implicit_invocation: false
