interface:
  display_name: "Infra Clickhouse"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $infra-clickhouse for this task."

policy:
  allow_implicit_invocation: false
