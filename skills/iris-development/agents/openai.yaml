interface:
  display_name: "Iris Development"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $iris-development for this task."

policy:
  allow_implicit_invocation: false
