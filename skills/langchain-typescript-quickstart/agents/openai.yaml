interface:
  display_name: "Langchain Typescript Quickstart"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $langchain-typescript-quickstart for this task."

policy:
  allow_implicit_invocation: false
