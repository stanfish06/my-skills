interface:
  display_name: "Next Cache Components Optimizer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $next-cache-components-optimizer for this task."

policy:
  allow_implicit_invocation: false
