interface:
  display_name: "Prisma Database Setup"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $prisma-database-setup for this task."

policy:
  allow_implicit_invocation: false
