interface:
  display_name: "Prometheus Cardinality Troubleshooter"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $prometheus-cardinality-troubleshooter for this task."

policy:
  allow_implicit_invocation: false
