interface:
  display_name: "Reconciler Logic"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $reconciler-logic for this task."

policy:
  allow_implicit_invocation: false
