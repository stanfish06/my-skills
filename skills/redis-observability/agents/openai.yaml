interface:
  display_name: "Redis Observability"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $redis-observability for this task."

policy:
  allow_implicit_invocation: false
