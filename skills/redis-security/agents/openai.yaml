interface:
  display_name: "Redis Security"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $redis-security for this task."

policy:
  allow_implicit_invocation: false
