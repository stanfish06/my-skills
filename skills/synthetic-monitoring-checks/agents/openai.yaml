interface:
  display_name: "Synthetic Monitoring Checks"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $synthetic-monitoring-checks for this task."

policy:
  allow_implicit_invocation: false
